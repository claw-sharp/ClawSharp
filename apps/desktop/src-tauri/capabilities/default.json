{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ClawSharp desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default", "dialog:allow-open", "updater:default"]
}
