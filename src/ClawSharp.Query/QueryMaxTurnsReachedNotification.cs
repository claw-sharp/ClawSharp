// TS parity status: ports the current TypeScript max_turns_reached attachment vocabulary as a focused C# query-runtime foundation contract.
namespace ClawSharp.Query;

public sealed record QueryMaxTurnsReachedNotification(
    int MaxTurns,
    int TurnCount);
